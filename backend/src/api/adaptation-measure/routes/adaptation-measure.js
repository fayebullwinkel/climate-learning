'use strict';

/**
 * adaptation-measure router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::adaptation-measure.adaptation-measure');
