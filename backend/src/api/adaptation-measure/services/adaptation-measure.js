'use strict';

/**
 * adaptation-measure service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::adaptation-measure.adaptation-measure');
