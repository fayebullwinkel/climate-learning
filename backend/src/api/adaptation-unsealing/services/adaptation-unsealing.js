'use strict';

/**
 * adaptation-unsealing service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::adaptation-unsealing.adaptation-unsealing');
