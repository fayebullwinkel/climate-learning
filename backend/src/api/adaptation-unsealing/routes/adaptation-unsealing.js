'use strict';

/**
 * adaptation-unsealing router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::adaptation-unsealing.adaptation-unsealing');
