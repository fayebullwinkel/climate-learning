'use strict';

/**
 * adaptation-unsealing controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::adaptation-unsealing.adaptation-unsealing');
